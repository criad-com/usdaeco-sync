# Sync protocol

See the [minimal example](../examples/minimal.usda), the [host contract](../../docs/host-contract.md), and the [use case](../../docs/usecase.md). The schema carries cached host bindings and transient findings; the engine owns layer transactions.

![Wall and pipe](usdAecoSyncExample.png)
